# AGENTS（新仓工作纪律）

1. 单真源：`governance/workflow_authority.json` 为 canon；数值只读 canon，本文件不复述数值。
2. 范围：止于 Idea 交付；禁 RQ/实验/代码执行/装依赖/写论文投稿；禁碰 `E:\Project`、`trae-input`、Codex/Trae 资产。
3. 单写者：`run.json` 唯一真源；预筛侧账 + reconcile 晋升；completed 不可裸改。
4. 写前：父目录 `Test-Path` 确认；路径 `-LiteralPath`；落盘统一 LF + 尾换行。
5. 反馈优先：28+ 存量 verdict 回填先于新供给；reject 必记失败账本。
6. 外部：borrow-not-merge；AGPL/GPL/NOASSERTION/无LICENSE 禁融；RAG/向量 dormant。
