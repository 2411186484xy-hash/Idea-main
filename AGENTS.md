# AGENTS（Idea Incubation OS v2 工作纪律）

> 哲学：**做对一次、吸收即删**（`docs/FOUNDATION-DESIGN.md` v2.0 定调）；**少而精**——以强性能与稳定为前提，不以少求稳。
> 定位：研究者主权的单机科研辅助工具，止于 idea 交付；认知判断归会话内模型，本地代码只做通道、门禁、审计、状态。

1. 单真源：`governance/workflow_authority.json` 为 canon；数值只读 canon，本文件与代码文档不复述数值。
2. 范围：止于 Idea 交付；禁 RQ/实验/代码执行/装依赖/写论文投稿；禁碰 `E:\Project`、`trae-input`、Codex/Trae 资产。
3. 生命周期：run 是临时工作状态，收口即删（session-log 留一行遗骸）；idea-pool 只留在途，交付即除名；派生视图不落盘；无冻结、无归档、无侧账——单一真源，不做同步。
4. 写前：父目录 `Test-Path` 确认；路径 `-LiteralPath`；落盘统一 LF + 尾换行；删除只删临时态，删前列目录确认。
5. 反馈优先：29 条存量 verdict 回填先于新供给；coverage 未达阈值不扩供给；verdict+reason 必填，reject 必记失败账本 lesson。
6. 外部：borrow-not-merge；AGPL/GPL/NOASSERTION/无LICENSE 禁融；RAG/向量 dormant；新机制先问"V1 验证过它的价值吗"。
