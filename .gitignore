# Runtime state: run dirs are deleted on finish, never committed
runs/

# Working scratch under the knowledge layer (extracts, big JSON dumps)
knowledge/.cache/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Never commit binaries or secrets (papers live in the canon paper root, not here)
*.pdf
*.png
*.zip
.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# WorkBuddy project data (memory/logs, not code)
.workbuddy/
